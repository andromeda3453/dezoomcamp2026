[project]
name = "module7"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kafka-python>=2.3.1",
    "pandas>=3.0.2",
    "psycopg[binary]>=3.3.4",
    "pyarrow>=24.0.0",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "pgcli>=4.4.0",
]
